/** Number of columns in the photo grid. */
export const COLS = 4;

/** Number of rows in the photo grid. */
export const ROWS = 3;

/** Total cell count (COLS × ROWS). */
export const CELL_COUNT = COLS * ROWS;
